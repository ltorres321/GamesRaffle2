@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom scrollbar */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #0f172a;
}

::-webkit-scrollbar-thumb {
  background: #22c55e;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #16a34a;
}

/* Base styles */
@layer base {
  html {
    scroll-behavior: smooth;
  }
  
  body {
    @apply antialiased;
  }
}

/* Component styles */
@layer components {
  .btn-primary {
    @apply bg-gradient-primary text-white font-semibold py-3 px-6 rounded-lg hover:shadow-lg transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed;
  }
  
  .btn-secondary {
    @apply bg-surface-light text-white font-semibold py-3 px-6 rounded-lg border border-dark-700 hover:border-primary-500 transition-all duration-200;
  }
  
  .btn-gold {
    @apply bg-gradient-gold text-dark-900 font-semibold py-3 px-6 rounded-lg hover:shadow-lg transition-all duration-200;
  }
  
  .card {
    @apply bg-surface-light border border-dark-700 rounded-xl p-6 transition-all duration-200;
  }
  
  .card-hover {
    @apply card hover:border-primary-500 hover:shadow-lg hover:shadow-primary-500/10;
  }
  
  .input-field {
    @apply w-full px-4 py-3 bg-surface-dark border border-dark-700 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:border-primary-500 focus:ring-1 focus:ring-primary-500 transition-colors;
  }
  
  .nav-link {
    @apply text-gray-300 hover:text-primary-400 font-medium transition-colors duration-200;
  }
  
  .nav-link-active {
    @apply text-primary-400 font-semibold;
  }
  
  .contest-card {
    @apply card-hover cursor-pointer transform hover:scale-105;
  }
  
  .progress-bar {
    @apply w-full bg-dark-800 rounded-full h-2 overflow-hidden;
  }
  
  .progress-fill {
    @apply h-full bg-gradient-primary transition-all duration-500 ease-out;
  }
  
  .team-card {
    @apply bg-surface-light border border-dark-700 rounded-lg p-4 cursor-pointer transition-all duration-200;
  }
  
  .team-card-selected {
    @apply team-card border-primary-500 bg-primary-500/10 shadow-lg shadow-primary-500/20;
  }
  
  .team-card-disabled {
    @apply team-card opacity-50 cursor-not-allowed border-red-500;
  }
  
  .badge {
    @apply inline-flex items-center px-3 py-1 rounded-full text-xs font-medium;
  }
  
  .badge-green {
    @apply badge bg-primary-500/20 text-primary-400 border border-primary-500/30;
  }
  
  .badge-gold {
    @apply badge bg-gold-500/20 text-gold-400 border border-gold-500/30;
  }
  
  .badge-red {
    @apply badge bg-red-500/20 text-red-400 border border-red-500/30;
  }
}

/* Animation utilities */
@layer utilities {
  .animate-fade-in {
    animation: fadeIn 0.5s ease-in-out;
  }
  
  .animate-slide-up {
    animation: slideUp 0.3s ease-out;
  }
  
  .animate-pulse-green {
    animation: pulseGreen 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
}

/* Custom animations */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideUp {
  from { 
    transform: translateY(10px); 
    opacity: 0; 
  }
  to { 
    transform: translateY(0); 
    opacity: 1; 
  }
}

@keyframes pulseGreen {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* Responsive design helpers */
@media (max-width: 640px) {
  .container {
    @apply px-4;
  }
}

/* Force visible text in registration form inputs */
input[type="text"],
input[type="email"],
input[type="tel"],
input[type="date"],
input[type="password"],
select {
  color: #000000 !important;
  background-color: #ffffff !important;
  -webkit-text-fill-color: #000000 !important;
  caret-color: #000000 !important;
  opacity: 1 !important;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="tel"]:focus,
input[type="date"]:focus,
input[type="password"]:focus,
select:focus {
  color: #000000 !important;
  background-color: #ffffff !important;
  -webkit-text-fill-color: #000000 !important;
  caret-color: #000000 !important;
}

input[type="text"]::placeholder,
input[type="email"]::placeholder,
input[type="tel"]::placeholder,
input[type="password"]::placeholder {
  color: #9ca3af !important;
  opacity: 0.7 !important;
}